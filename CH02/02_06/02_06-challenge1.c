#include <stdio.h>

int main() {
  int a = 5;
  const char b = 'b'; // Remember that const must be assigned immediately

  printf("Here is the value of A: %d\n", a);
  printf("Here is the value of B: %c\n", b);

  return (0);
}
